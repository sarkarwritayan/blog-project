import BlogComponent from '@/Components/BlogComponent';
import MainLayout from '@/Components/MainLayout';

const Home = () => (
    <MainLayout>
        <BlogComponent />
    </MainLayout>
);

export default Home;